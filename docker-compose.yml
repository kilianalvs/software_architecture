version: '3.8'

services:
  frontend:
    build:
      context: ./front
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    container_name: frontend-container
    networks:
      - app-network

networks:
  app-network:
    driver: bridge